# Лабораторная работа 1. 1. Установка и настройка Docker. Работа с контейнерами в Docker

## Цель работы

Освоить процесс установки и настройки Docker, научиться работать с основными командами CLI, контейнерами и образами. Понять принципы контейнеризации для развертывания аналитических сред и сервисов.

## Шаг 1. Проверка установки Docker 

Убедимся, что докер установлен корректно с помощью команды:

```
docker --version
```

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20204343%20—%20копия.png" />

Также запустим тестовый контейнер:

```
docker run hello-world
```

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20204510%20—%20копия.png" />

## Шаг 2. Знакомство с командами Docker CLI

Посмотрим все скачанные образы с помощью команды:

```
docker images
```

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20204618%20—%20копия.png" />

Видим, что среди скачанных образов есть последняя версия grafana, которая будет использоваться в индивидуальном задании

Затем посмотрим все запущенные контейнеры:

```
docker ps
```

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20204631%20—%20копия.png" />

И все контейнеры, включая остановленные:

```
docker ps -a
```

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20204640%20—%20копия.png" />

# Шаг 3. Индивидуальное задание

Вариант 12 - grafana/grafana.	Дашборды. Запустить контейнер, открыть веб-интерфейс (порт 3000), зайти под admin/admin. Изучить раздел добавления источников данных (Data Sources).

Создадим исполняемый файл:

```
nano start_grafana.sh
```

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20204754%20—%20копия.png" />

Напишем код для запуска grafana с указанием порта и названия контейнерв:

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20205644%20—%20копия.png" />

Результат:

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20205818%20—%20копия.png" />

Дадим файлу разрешение на исполнение и запустим скрипт с помощью команд:

```
chmod +x start_grafana.sh

./start_grafana.sh
```

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20205827%20—%20копия.png" />

Запуск был выполнен успешно, так как образ grafana уже был в локальном хранилище, докер его запустил и вывел идентификатор контейнера.

Убедимся, что наш контейнер запущен:

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20205931%20—%20копия.png" />

Видим, что в списке есть контейнер grafana-lab.

Откроем браузер и перейдем по адресу localhost:3000. Откроется grafana, войдем под admin/admin:

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20210215%20—%20копия.png" />

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20210301%20—%20копия.png" />

Изучим раздел Data Sources:

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20210301%20—%20копия.png" />

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20210415.png" />

Остановим и удалим контейнер:

<img width="800" height="500" alt="image" src="/lab1/images/Снимок%20экрана%202026-02-10%20210627.png" />

## Выводы

В ходе выполнения лабораторной работы были изучены основные команды CLI, а также команды для работы с контейнерами и образами. Кроме того, были получены практические навыки - запущен контейнер grafana на порту 3000 и изучен один из разделов данного инструмента аналитики.
